name: Pubblica Rassegna SAVT su GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configura GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Prepara sito leggero
        shell: bash
        run: |
          rm -rf _site
          mkdir -p _site/assets
          cp index.html _site/index.html
          cp -R archivio _site/archivio
          touch _site/.nojekyll
          base64 -d assets-src/logo.webp.b64 > _site/assets/logo.webp
          base64 -d assets-src/europa.webp.b64 > _site/assets/europa.webp
          base64 -d assets-src/italia.webp.b64 > _site/assets/italia.webp
          base64 -d assets-src/valle.webp.b64 > _site/assets/valle.webp

      - name: Carica sito
        uses: actions/upload-pages-artifact@v3
        with:
          path: '_site'

      - name: Pubblica
        id: deployment
        uses: actions/deploy-pages@v4
